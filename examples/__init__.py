"""Example scripts for the NN framework.

This file makes `examples/` a package so you can run examples as modules:

  python -m examples.xor_binary

Running as a module keeps the repo root on `sys.path`, which allows
`import NN` to work without setting `PYTHONPATH=.`.
"""
